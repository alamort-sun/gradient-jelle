# gradient-jelle — Jelle (JEPA+LLM+MoE) build plan

STATUS: sandbox access fence on existing-file open/unlink in
projects/gradient-jelle/. New-file creation OK; read/edit of Cargo.toml + lib.rs
BLOCKED. cargo build/test DEFERRED until the window clears.

## Direction 1 — codec pin (BLOCKED, edit on resume)
projects/gradient-jelle/Cargo.toml — add under [dependencies]:
  vecGradient = { path = "../gradient-codec/vecGradient" }
Upstream alt (no local checkout):
  vecGradient = { package = "vecGradient", git = "https://github.com/alamort-sun/gradient-codec", rev = "c8d0ee1" }
Provides: vecGradient::Vector15D / DomainWall / GaugeCoupling / try_new_15 / validate().

## Direction 2 — Jelle pipeline
projects/gradient-jelle/src/lib.rs — add one line:
  pub mod jelle;
    pub use jelle::{Jelle, JelleState, ExpertIndex, LlmCondition, StepOutcome, JelleError};
New file authored: projects/gradient-jelle/src/jelle.rs
Main class: Jelle (the JEPA+LLM+MoE orchestrator). Uses the 5 existing gate
modules + the codec (via JelleState -> vecGradient::Vector15D). No invented methods
on the locked modules; existing types are constructed with their known fields.

## Verify on recovery (shell with a valid CWD):
  cd projects/gradient-jelle
  cargo build
  cargo test            # keep 15/15 storm gates + new Jelle tests
  cargo build --manifest-path ../gradient-codec/vecGradient/Cargo.toml
