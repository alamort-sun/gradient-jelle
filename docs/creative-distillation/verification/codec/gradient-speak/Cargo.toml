[package]
name = "gradient-speak"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Rendering pipeline: Vector13D → SVG glyph output with torsion-field spacing"

[dependencies]
vector13d = { path = "../vector13d" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
