[package]
name = "gradient-jelle"
version = "0.1.0"
edition = "2021"
description = "JEPA student of the Gradient stack. Susano storm gates live here until Saraswati ships."
license = "PolyForm-Noncommercial-1.0.0"

[lib]
name = "gradient_jelle"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
vecGradient = { path = "../gradient-codec/vecGradient" }
